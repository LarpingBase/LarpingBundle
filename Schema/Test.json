{
  "$id": "https://larping.nl/test.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": "0.2",
  "title":"Setting",
  "type":"object",
  "nameProperties": ["description"],
  "description":"The setting is the highest form of abstraction within larping base, it ties together stories, characters and all their quirks into storyarcs usually spanning several events.",
  "properties":{
    "description":{
      "type":"string",
      "description":"Vampiric pirates that heal people by replacing their blood with rum"
    }
  },
  "required":[
    "description"
  ]
}
